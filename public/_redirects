# External Redirects:
/tos       https://exotek.co/tos   303
/privacy   https://exotek.co/privacy   303

# Internal Rewrites (200 status makes them stay on the current URL): (DISABLED)
# /join      /join.html    200
# /export    /export.html   200
# /tos       /tos.html      200
# /privacy   /privacy.html  200
# /embed     /embed.html    200

# Force a true 404 for missing static chunks (prevents HTML-as-JS error)
/assets/*  -  404

# SPA Catch-all (Must be last):
# This handles /join, /export, /embed, AND React/Vue routing.
# Cloudflare will check if /join.html exists first; if not, it hits this.
/*         /index.html    200